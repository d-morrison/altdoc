[lint]
# Test fixtures under tests/testthat/examples/ are throwaway packages used
# to exercise altdoc's conversion logic; they intentionally contain code
# (e.g. unused functions) that would be flagged in real package source.
exclude = ["tests/testthat/examples/"]
