name: Gemini Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'Pull request number to review'
        required: false
        type: string

jobs:
  dispatch-on-comment:
    if: >-
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request &&
      (startsWith(github.event.comment.body, '/gemini-review') || startsWith(github.event.comment.body, '/review-gemini')) &&
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
    runs-on: ubuntu-latest
    permissions:
      actions: write
      issues: write
    steps:
      - name: Dispatch review
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.issue.number }}
        run: |
          gh workflow run "gemini-code-review.yml" --repo "$REPO" -f pr_number="$PR_NUMBER"
          gh issue comment "$PR_NUMBER" --repo "$REPO" \
            --body "🔍 Received request -- dispatched Gemini code review for PR #$PR_NUMBER."

  review:
    if: github.event_name != 'issue_comment'
    # Serialize reviews per PR so a rapid string of pushes (e.g. an
    # unattended automated session) doesn't fire N overlapping reviews.
    # cancel-in-progress: true means only the latest push's review survives
    # -- the earlier ones are canceled rather than piling up and each
    # separately consuming the API quota. This is part of the remediation
    # for the 2026-07-29/30 Gemini API suspension incident: this workflow
    # re-triggered on every synchronize event with no debounce at all,
    # repeatedly exhausting the free tier's 20-requests/day quota over more
    # than 20 hours straight.
    concurrency:
      group: gemini-review-${{ github.event.pull_request.number || inputs.pr_number }}
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Run Gemini PR Review
        uses: google-github-actions/run-gemini-cli@v0
        with:
          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
          prompt: "Please perform a code review on this pull request and provide clear feedback on design, correctness, formatting, and performance."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GEMINI_CLI_TRUST_WORKSPACE: 'true'
